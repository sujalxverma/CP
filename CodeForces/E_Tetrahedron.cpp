#include "bits/stdc++.h"
using namespace std;
using ll = long long;
const ll mod = 1e9 + 7;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    ll n;
    cin >> n;
    /*
     minimum number of steps to return to D, is 2.
     n-1 step cannot land at D.

    */

    return 0;
}