#include "bits/stdc++.h"
using namespace std;
using ll = long long;
const ll mod = 1e9 + 7;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    ll total = 1;
    ll n,m;
    cin >> n >> m;
    vector<ll>a(n);
    for(ll i = 0; i < n ; i++) cin >> a[i];

    for(ll i = 0 ; i < n ; i++){
        if(i == 0){
            if(a[i] == 0){
                
            }
        }else if(i == n-1){

        }
    }
    
    return 0;
}