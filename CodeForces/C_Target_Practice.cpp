#include "bits/stdc++.h"
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while(t--){
        int n;
        vector<vector<char>>g(10 , vector<char>(10));
        for(int i = 0; i < 10 ; i ++){
            for(int j = 0; j < 10 ; j ++){
                cin >> g[i][j];
            }
        }

        int ans = 0;

        for(int row = 0; row <= 4 ; row ++){

            for(int col = )
        }

    }
    return 0;
}