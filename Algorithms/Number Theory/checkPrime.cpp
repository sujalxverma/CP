#include <iostream>
using namespace std;
/*
-To check if a number is prime or not.
*/
bool checkPrime(int n)
{
    if (n < 2)
        return 0;
    for (int i = 2; i * i <= n; i++)
    {
        if (n % i == 0)
        {
            return 0;
        }
    }
    return 1;
}