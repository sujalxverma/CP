#include "bits/stdc++.h"
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while(t--){
        int n,p;
        cin >> n >> p;
        int count = 0;
        for(int i = 1 ; i <= p ; i++){
            for(int j = 1; j <= p ; j++){
                for(int k = 1 ; k <= p ; k++){
                    if( ((((n%i)%j)%k)) =   )
                }
            }
        }
    }
    return 0;
}